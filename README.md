# github-workflows

Use Github Actions Workflows to deploy two nacos and zookeepers.

## Nacos

The workflow is named `nacos-lifecycle.yml`, which can run on both **Linux** and **Windows** successfully.

## Zookeeper

The workflow is named `zookeeper-lifecycle.yml`, which can run on both **Linux** and **Windows** successfully.